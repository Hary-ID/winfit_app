import 'package:flutter/material.dart';

class StokOpnameScreen extends StatelessWidget {
  const StokOpnameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Stok Opname")),
      body: const Center(
        child: Text("Halaman Stok Opname"),
      ),
    );
  }
}
